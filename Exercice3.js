"use strict"

let names = [];

names.push("Vincent");
names.push("Paul");
names.push("Arthur");

for (let i = 0; i < names.length; i++)
{
    names[i] += " Va à la péche" ;
    console.log(names[i]);
};